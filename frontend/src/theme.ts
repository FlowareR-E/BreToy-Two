import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1DB954', 
      light: '#1ED760', 
      dark: '#0F4D0F', 
      contrastText: '#ffffff' 
    },
  },
});

export default theme;